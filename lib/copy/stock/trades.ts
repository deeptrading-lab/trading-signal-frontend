/**
 * 체결강도 + 체결 테이프 한글 카피 — PRD `toss-trades` §3-3b, DESIGN.md 상태 매트릭스 R3/R7.
 *
 * 근사(추정) 성격을 정직하게 표기하는 카피가 핵심(§9 q1) — 큰 경고 대신 "추정치" 미세 칩 + 툴팁.
 * 색·간격 토큰은 컴포넌트가 소유. i18n 여지로 단일 위치 유지.
 */

export const TRADES_COPY = {
  /** 패널 헤더 제목. */
  title: "체결강도",
  /** 체결강도 블록 소제목. */
  strengthLabel: "체결강도",
  /** 체결 테이프 블록 소제목. */
  tapeLabel: "최근 체결",
  /** 매수 우위 readout 접두. */
  buy: "매수",
  /** 매도 우위 readout 접두. */
  sell: "매도",
  /** 산출 불가(강도 불명) 표기. */
  dash: "—",
  /** 근사 안내 미세 칩. */
  approxChip: "추정치",
  /** 근사 안내 툴팁(과신 차단 — 정확 수급 아님). */
  approxTooltip: "체결 방향 추정치 · 실제 수급과 다를 수 있어요",
  /** 강도 불명(분모 0·전부 동일가) 안내. */
  strengthUnknown: "체결 방향을 추정할 수 없어요",
  /** 빈 체결 공통 제목. */
  emptyTitle: "체결 내역이 없어요",
  /** 장 마감(키 있음·빈 응답) 보조 문구. */
  emptyClosed: "장이 열리면 실시간 체결을 볼 수 있어요.",
  /** 미지원/미존재 종목 보조 문구. */
  emptyUnsupported: "이 종목은 체결 정보를 제공하지 않아요.",
} as const;
