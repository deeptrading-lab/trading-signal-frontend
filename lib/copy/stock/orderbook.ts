/**
 * 호가창(매수/매도 잔량) 한글 카피 — PRD `toss-orderbook` §3-5, DESIGN.md 상태 매트릭스 R6.
 *
 * 빈 호가(장 마감/미지원)·헤더·요약 라벨 단일 위치(i18n 여지). 색·간격 토큰은 컴포넌트가 소유.
 */

export const ORDERBOOK_COPY = {
  /** 패널 헤더 제목. */
  title: "호가",
  /** 매도 총잔량 라벨. */
  totalAsk: "총매도",
  /** 매수 총잔량 라벨. */
  totalBid: "총매수",
  /** 스프레드 라벨. */
  spread: "스프레드",
  /** 스프레드/현재가 산출 불가 표기. */
  dash: "—",
  /** 빈 호가 공통 제목. */
  emptyTitle: "호가 정보가 없어요",
  /** 장 마감(키 있음·빈 응답) 보조 문구. */
  emptyClosed: "장이 열리면 매수·매도 잔량을 볼 수 있어요.",
  /** 미지원/미존재 종목 보조 문구. */
  emptyUnsupported: "이 종목은 호가를 제공하지 않아요.",
} as const;
