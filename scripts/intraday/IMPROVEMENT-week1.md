# 단타 판단 개선 — Week1 계획 & 현황

브랜치 `feature/hylee/intraday-improvement-week1` 전용 워킹 문서. 이번 주 매일 장 마감 후
`daily.mts`(provider별)·`pool.mts`(누적 풀링) 돌리고 하단 **일일 로그**에 결과·관찰을 추가한다.
개선 실험도 이 브랜치에서만 하고, 검증되면 main 에 추후 머지.

## 진단 (5일 라이브, 7/10~7/20, 2모델 교차)

- **진입에 양의 엣지가 없음**: 실체결 100매수 **승률 24%** (RRR≈2 손익분기 33% 미달) → 5일 **−777k**(claude −675k·codex −102k).
- **conviction 무스킬**: convSpear −0.03 (n=1,344). **고확신(≥65)이 중간(50-64)보다 나쁨** — ≥65 18% vs 50-64 27% (풀링).
- **고확신 실패가 중립 regime 집중**: ≥65 승률 **중립 11% vs 강세 25%**. 횡보주에서 강도 사서 평균회귀.
- **4축 신호도 약한 역**: sigSpear −0.10 (claude·codex 공통) → 판단 스택 전체가 방향을 못 짚음.
- **청산 37손절 : 18익절** (2:1).
- ★ claude·codex 두 모델 동일 패턴 = 특정 모델 아닌 **접근법(4축 신호/judge 구조) 문제**.

## 플랜

### P0 ✅ 완료 (main 머지 #368, 829d84d)
청산 rationale 표기버그(리스크 스윕 강제청산이 '청산 조건 미도달'로 오표기, 매도 64%). 매매 무영향·표기만.

### P1a — 중립 regime 고확신 진입 억제 → ⏸️ 보류(패치지 해결 아님)
- **가설**: 중립 regime ≥65 BUY 가 per-tick 11%로 최악 → 막으면 손실↓.
- **검증 방식**: 정식 백테스트는 불가(① conviction=LLM 이라 과거 재실행 불가 ② 과거 분봉 데이터 없음 —
  KIS/Toss 최근 며칠만). → **모은 데이터의 반사실 재점수**(A/B 아님·forward A/B 는 게이트 on/off 라이브 병렬).
- **결과(regime 정상 3일 7/14·15·20, 실제 트레이드 60건)**: 게이트 시 손익 −290k→−209k(+80k), 그러나
  **승률 21.7%→21.7% 불변**·**여전히 net 손실**. 실제 진입 60건 전부 ≥65(=BUY 컷), 중립 14·강세 46.
  제거된 중립 14건 승률 21.4%≈전체 → 실제 트레이드 레벨에선 중립 특이성 사라짐(per-tick 11%는 미진입 틱 포함).
  **강세≥65만 남겨도 −209k·21.7% = 최선 조건에서도 손실.**
- **판정**: 게이트는 손실 ~28% 줄이나(노이즈 가능) **흑자 전환 못 함. regime 이 아니라 진입 자체가 무엣지.**
  구현 ROI 낮음 → **보류, P2 로.** (스크립트 scratchpad `gate-cf.mjs`)

### P1b — 손절폭/RRR 재검토
- **질문**: 37손절이 **조기 손절**(흔들려 나간 뒤 목표 도달)인가 **방향 오류**(계속 하락)인가?
- **분석**: 손절된 진입마다 손절 후 세션 내 목표가에 닿았는지 추적. 조기 다수 → SL 완화/RRR 하향 테스트. 방향 오류 다수 → 진입 자체 문제(P2).

### P2 — 신호/judge 재설계 or 전략 피벗 (P1 결과 후)
- sigSpear 음 = 4축 신호 토대가 역. conviction judge 는 고확신에서 오히려 해로움.
- 옵션 A: 강도 추종 → **페이드/눌림 확인**. B: 4축 재구성. C: 자동 진입 폐기 → **decision-support(사람 집행)** 피벗(원 스캘핑 NO-GO + 5일 −777k 재확인).

## 도구

- `daily.mts` (main) — 매일 provider별 1커맨드. `output/daily-log.tsv` 누적.
- `pool.mts` (이 브랜치) — 저장 5일치 provider별 풀링(큰 표본).
- 실험 스크립트는 이 브랜치 `scripts/intraday/` 에 추가.

## 일일 로그

승률/상관 = 반사실(labelTick) · 손익분기 ~33% · convSp/sigSp = vs +30분 forward return.

| 날짜 | claude grand/≥65/convSp/sigSp | codex grand/≥65/convSp/sigSp | regime(−/0/+) | 관찰 |
|---|---|---|---|---|
| 7/10 | 23.2 / 7.7 / −0.09 / −0.17 | 32.2 / 14.3 / −0.05 / −0.01 | claude 0/all/0 · codex 동 | regime veto 무력화(픽스 前) |
| 7/13 | 15.9 / 16.7 / −0.16 / −0.36 | *대량실패(codex 646에러)* | 0/all/0 | codex CLI 미설치 노이즈 |
| 7/14 | 27.9 / 15.4 / +0.01 / −0.14 | 31.7 / 33.3 / −0.08 / +0.05 | 3/83/60 | ★regime 픽스(#353) 라이브 |
| 7/15 | 23.4 / 18.5 / −0.08 / −0.14 | 26.2 / 17.9 / −0.16 / −0.17 | 0/38/184 | veto 안정 |
| 7/20 | 38.3 / 50.0 / +0.03 / +0.03 | 12.0 / 7.7 / +0.03 / +0.01 | 0/43/46 | claude 좋고 codex 나쁨=노이즈 |
| _다음_ | | | | _P0 픽스 후 첫 클린일_ |

**주간 관전 포인트**: ① 어느 모델이든 ≥65 가 손익분기(33%) 꾸준히 넘는가 ② convSpear 양전 유지 ③ 청산 rationale 정상 표기(P0). 안 넘으면 P1 검증 → P2 재설계.

---

## 최종 개선 계획 (2026-07-21 — 반사실 3종 + 부품해부 + 선행정보 리서치 후)

### A. 데이터로 배제된 것 (더 안 팜)
- **P1a 중립regime 게이트**: 흑자 못 만듦(강세≥65도 net 손실). 보류.
- **P1b 손절폭**: 좁혀도(1.0×) 넓혀도(3.0×·무손절) net 악화(−0.86%→−1.44%). 물타기(마틴게일)는 승률 55% **착시** — 여전히 net 음수 + 꼬리위험. 보류. (스크립트 `stop-cf.mts`·`stop-sweep.mts`·`martingale.mts`)
- **부품 해부(`signal-decompose.mts`)**: 4축(추세/모멘텀/거래량/변동성) **전부 forward return 음/무상관**(−0.04~−0.09) → 재가중할 "좋은 축" 없음. structureEvent(돌파 셋업)도 오히려 약간 나쁨. 시간대만 갈림(오전 +0.19%/오후 −0.80%)이나 **시간 차단은 문제 회피지 판단 개선 아님**(오후를 영영 안 할 것 아니므로).

### B. 선행정보 리서치 결론 (★기대치 현실화)
- order flow(호가 불균형·CVD·체결강도)가 "선행" 도메인은 맞음. 근데 학술+실무 근거: **예측 지평 5~30초·거래비용 못 이김**([arxiv 1901.10534](https://arxiv.org/pdf/1901.10534)·[electronictradinghub](https://electronictradinghub.com/leveraging-limit-order-book-imbalances-for-profitable-trading-a-deep-dive-into-recent-research-and-practical-tools/)). 체결강도도 실무자 한계 명시(저거래량 왜곡·심리 지배).
- **우리 5분틱 + CLI 25초 지연과 지평 불일치** → 선행정보는 "**알파**"가 아니라 "**가짜돌파 필터(거부권)**"로만 가치. **강한 엣지는 이 속도/피처로 불가**가 정직한 결론.

### C. 주기 단축 = 기각
- edge는 초 단위(LLM 지연으로 도달 불가) · 짧은 봉=노이즈(과거 2분=1분봉 노이즈 확인) · CLI 과부하. **5분 유지**(오히려 스윙 쪽 검토가 방향).

### D. 실제 시도할 개선 (기대치 = 손실감소/본전, 강한 엣지 아님)
1. **I1 일봉 컨텍스트 강화** ✅ **구현 완료(de95c56)** — `lib/signal/dailyContext.ts`: MACD 골든/데드크로스·RSI(과매수/과매도)·이평 배열(정/역)·전고/전저 대비 위치·최근5일 추세를 요약해 judge 프롬프트 `[일봉 흐름]` 블록으로 주입(기존 레짐 한 줄 → 상세). buildContext 재사용·당일 캐시. 실데이터 검증 OK(삼성=조정·모나미 RSI92 과매수 등). **⚠️ 라이브 반영=dev 재시작 필요** → 다음 5일 `daily-log` 로 선택·스탠스 개선(≥65WR·net) 관찰.
2. **I2 셋업 우선순위 + 충돌 규칙** ✅ **구현 완료(b0ad7b7)** — `JUDGE_SYSTEM` 에 `[★진입 우선순위]`(A+ 매수 셋업 3종=눌림목/VWAP 재탈환/눌림 후 돌파, 각각 I1 일봉 흐름과 정렬 전제) + `[충돌 해소]`(트리거·거부권 공존 시 **거부권 우선→관망**, A+ 정렬+거부권 없음만 65↑). 거부권=일봉 과매수/역행·고점추격·위꼬리·가짜돌파·저점붕괴. analyst 도 정렬. "평균 종합"→"소수 A+ 셋업 정렬"(평균이 엣지 희석하던 것 직격). **⚠️ dev 재시작 후 5일 검증**.
3. **I3 선행정보 = 필터** ✅ **구현 완료(ce00ba5)** — `lib/server/paperTrading/orderFlowContext.ts`: 토스 체결강도(틱룰)·호가 잔량 불균형을 요약해 judge 에 `[수급 선행]` 블록 주입. LLM 호출 경로에서만 warnings 와 병렬 fail-soft 조회(preGate 스킵 틱 미조회). JUDGE_SYSTEM 거부권에 "체결강도/호가 매도 우위=흡수/가짜돌파" 추가·A+ 셋업은 수급 매수 우위로 확인. 알파 아닌 필터(초단위 노이즈 단독 X). 실데이터 검증 OK(삼성 체결강도63·SK이노 80). **⚠️ dev 재시작 후 5일 검증**.

### E. 검증 방식
- 백테스트 불가(LLM 과거 재실행 불가 + 과거 호가·분봉 데이터 없음) → 각 개선을 이 브랜치에 구현 → **5일 라이브** → `daily-log.tsv`(convSpear·≥65WR·net) 개선 확인. (forward A/B 는 개선 on/off 라이브 병렬이 이상적이나 세션 수 제약.)

### F. ★ 정지 조건 (무한 반복 방지)
- I1~I3 다 적용 후에도 여러 사이클 **forward skill 0/음수 · 손익분기(33%) 미달**이면 → **자동 진입 접고 decision-support / 스윙 호라이즌으로 전환**. (원 스캘핑 NO-GO + 이번 주 결론 존중. sunk cost 방지.)

### G. 순서
**I1(싸고 명확) → I2(A+ 셋업 설계) → I3(호가/체결강도 배선)** — 각각 구현 → 5일 → 측정. 한 번에 하나씩(무엇이 효과냈는지 격리).

## 실행 로그

### 7/21(화) — I1·I2·I3 적용 첫날 (하영 claude)
- ✅ **작동 확인**: rationale에 I1(일봉 RSI/20일고점 위치)·I2(트리거 vs 거부권→관망)·I3(체결강도/호가 우위) 다 라이브 반영. judge 실패 minimal, regime 정상(강세30/중립28/약세13).
- **① 고무적(반사실)**: convSpear **+0.107**·sigSpear **+0.163**(처음 뚜렷 양전) · 버킷 **단조 증가**(<40 33% < 40-49 32% < 50-64 41%, 베이스라인 역상관 뒤집힘) · grand 35.8%(손익분기 위).
- **② 과선별**: conviction 평균 46.8 · **최대 64에서 막혀 65컷 미달 → HOLD 100%, 실매매 0건**. veto가 분포를 통째로 하향.
- ⚠️ **냉정**: 1일 + 반사실(실매매 0) + 찬민 codex도 convSpear +0.20(개선 미적용인데도) → 7/21이 원래 잘 먹힌 장세였을 가능성. 양전을 개선 덕으로만 돌리긴 이름.
- **다음**: 1일 더 관찰 → 같은 패턴(64 막힘 + 반사실 양수)이면 **BUY 컷 `INTRADAY_BUY_CONVICTION_MIN` 65→60** 내려 50-64 밴드(반사실 41%)에서 실매매 열기(실현 데이터 축적). 컷은 상수 한 줄.

### 7/22(수) — 2일차 + BUY 컷 65→58 재보정
- **패턴 반복**: 여전히 과선별(conviction 평균 43.5·최대 68·실매매 ~0). convSpear **+0.112**·sigSpear **+0.258**(2일 연속 양전).
- ⚠️ **오늘 grand 12%는 장세**(찬민 codex도 12.4% — 두 모델 다 안 좋은 날). ★attribution 문제: convSpear 양전을 개선 덕으로 못 잘라 말함(codex도 양전, 7/21엔 codex +0.20 > claude +0.11).
- **결론=매매를 안 해서 검증 불가** → **BUY 컷 65→58 재보정(6683b62)**. 근거: 개선 2일 claude conviction 분포 평균 44.9·P82=58(컷 58=상위 18% 통과·30틱). 65는 1%(2틱)뿐이라 실현 데이터가 안 쌓임. 58로 열어 **50-64 밴드가 실매매→진짜 손익**을 측정. 사이징 자동조정(58→20%), 테스트 갱신, vitest216.
- **다음**: dev 재시작 후 컷 58로 며칠 → **실현 net(반사실 아님)** 이 손익분기 넘는지. 넘으면 개선 유효, 계속 손실이면 정지조건(피벗). ★58도 절대적 아님 — 실매매 결과 보고 재조정.

### 7/28(화) — ★원인 규명: 과선별의 진짜 범인은 **내 프롬프트의 컷 숫자**
컷을 58로 내려도 매매가 안 열려 분포를 전수 재확인(7/21~23 v2 449틱):

| 구간 | 40-49 | 50-54 | 55-57 | **58-61** | **62-64** | 65-69 | 70+ |
|---|---|---|---|---|---|---|---|
| 비중 | 19% | 7% | 11% | **22%** | **25%** | 4% | 0% |

- **62-64 에 25%, 58-64 에 47% 밀집 · ≥65 는 4%뿐 = 자연 분포가 아니라 명확한 천장.** 7/23 은 최대값이 **정확히 65**.
- **범인 = I2 프롬프트**: `"높은 매수 확신(65↑)은 A+ 셋업일 때만"` / `"A+ 정렬 → 65~85"` 로 **컷 숫자를 못 박음** → judge 가 65를 "넘어선 안 되는 벽"으로 학습, A+ 완벽 정렬 아니면 62-64 에 눌러 담음. 게다가 코드 컷은 58 인데 프롬프트는 65 를 말해 **코드·LLM 이 서로 다른 기준**으로 판단.
- ⚠️ **자기수정**: 7/22 에 "오답노트(#371 머지) 때문에 보수적"이라 추정했으나 **오답노트 도입 전 7/23 에 이미 65 천장**이 존재 → 오답노트는 범인이 아님. 다만 오답노트 규칙이 문구로 `BUY컷`을 참조해(`"그전 점수는 BUY컷 아래"`·`"conviction≥BUY컷"`) **컷을 바꾸면 규칙 의미가 조용히 변하는** 별개 문제가 있었음(65→58 때 실제로 발생).

**픽스 (2eaac49) — 원칙 복원(PR-3a): LLM 은 셋업 품질만 점수화, 컷·사이징·게이트는 코드. 프롬프트·오답노트 어디에도 컷 숫자가 없어야 한다.**
1. **프롬프트 밴드 재배치 + 앵커 제거**: A+ 정렬 **75~89**(정렬+거래량+수급이면 90+) · **단일 근거 60~74**(새로 명시 — "근거 하나뿐이라고 중립으로 누르지 말 것") · 엇갈림 45~55 · 거부권만 ≤35. 여기에 `"특정 점수를 매수 문턱으로 의식하거나 그 근처에서 멈추지 마세요"` 명시.
2. **오답노트 규칙 문구 정성화**: `BUY컷 아래`→`매수 확신을 낮게 유지`, `conviction≥BUY컷`→`높은 매수 확신 구간`, `컷만 재충족`→`확신 점수만 재충족`. derive.ts + source JSON 수정 후 `npm run merge` 로 CM.md 재생성(규칙 ID·SHADOW 등급·교훈 내용은 그대로 — 교훈 자체는 유효).
- **컷 58 유지**(내리지 않음). 밴드가 퍼지면 컷경계에 진짜 애매한 것만 남아 오답노트 ENTRY 규칙("확신 애매 시 확인 전 낮게")이 **의도대로** 작동 — A+ 는 75~89 로 브레이크 사정거리 밖.
- **검증 설계**: 60~74(단일 근거)와 75~89(A+) **두 구간이 모두 매매**되게 해서 → "A+ 정렬이 실제로 더 이기는가"(**I2 가설 자체**)를 실현 손익으로 판정 가능. 지금은 47%가 한 칸에 뭉쳐 변별력 0.
- ⚠️ **사이징 부수효과**: `20+(conv−58)×2` 라 conv 75↑ 는 maxPositionPct(50%) 캡에 상시 도달. 의도된 방향(A+ 에 최대 비중)이나 라이브에서 체결 규모 확인 필요.
- **다음**: dev 재시작 → 평범한 장에서 **분포가 실제로 퍼지는지**(62-64 벽 소멸·75+ 등장) 먼저 확인 → 그다음 두 구간 실현 승률 비교.

### 7/29(수) — 약세일. 벽은 깨졌으나 A+ 미검증 → **약세 관측 창** 구현
상세는 [MONITOR-2026-07-29.md](MONITOR-2026-07-29.md). 요약: 에러0·로테이션 33종목·**62-64 25%→2%**·최대 66(65 천장 돌파)·BUY 5건 전부 신설 60~74 밴드. 단 분포가 아래로 쏠려(평균 39.2) 75-89는 0건 — 약세 regime 교란으로 결론 유보. 실매매 3건 전패 −33,475원(표본 3=통계 아님).
→ **`7152b1f` 약세 관측 창**: `evaluatePreGate` 에 `regime===-1 && !noNewEntry` → LLM 호출 예외. 사후 게이트(레짐 veto) 무변경이라 진입 위험 불변, 판단 기록만 확보.

### 7/30(목) — ★관측 창 작동 + **비용이 손실의 74%**라는 결정적 분해
상세는 [MONITOR-2026-07-30.md](MONITOR-2026-07-30.md).

**① 픽스는 기계적으로 전부 성공**

| 지표 | 7/29 | 7/30 | |
|---|---|---|---|
| AI 호출(하영) | 98 (35%) | **151 (73%)** | ✅ 관측 창 작동 |
| conviction 평균 | 39.2 | **49.5** | 정상화 |
| 최대 | 66 | **76**(전체 78) | ✅ 65 천장 완전 소멸 |
| **75-89** | 0건 | **2건** | ✅ **A+ 밴드 첫 등장** |
| 컷 58 통과 | 12% | **40%** | 매매 열림 |
| AI 액션 | HOLD/BUY | +**EXIT 6** | ✅ 청산 판단 등장 |
| 에러 | 0 | **0** | |

**② 그런데 실현은 −58,593원(−0.586%). 그 원인 분해가 핵심:**
```
실현 −57,178원  =  순수 가격손익 −14,721원  +  거래비용 −42,457원
                                                  ★손실의 74%
```
- **AI가 잡는 가격 이동이 사실상 0(−0.1%)인데 왕복비용 0.28%를 매번 낸다.** 베이스라인(−0.24%/틱)과 **같은 구조적 문제** — 판단이 나빠서 크게 잃는 게 아니라, **비용을 넘는 움직임을 못 잡는다**.
- 손익 구조를 뒤집으려면 **거래당 평균 +0.4%p** 개선 필요(현재 −0.1% → 필요 +0.28% 초과).
- 청산 사유: **손절선 8 / 익절 2 / 판단 7 / 장막판 1** — 손절:익절 = 4:1.

**③ 판정 유보 — 표본 부족.** 하영 누적 라운드트립 **11건**(7/29 3 + 7/30 8). 목표 20~30의 절반.
⚠️ **여기서 튜닝하지 않는다**: 손절 폭 확대는 지난주 반사실 재시뮬에서 **net 악화**로 이미 기각됐고(승률 24→38%인데 net −0.86%→−1.44%), 선별 강화는 매매 0건 문제로 회귀. 하루치 눈짐작 금지 원칙 유지.
- **다음**: 컷·밴드·손절 **동결**한 채 20~30 라운드트립까지 누적. 매일 `npx tsx scripts/intraday/today.mts` 로 비용 분해·청산 사유를 기록. 그때 "가격손익이 비용을 넘는가"로 판정.
