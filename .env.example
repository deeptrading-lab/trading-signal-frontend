# === Frontend (Next.js) ===
#
# 로컬 환경변수 템플릿.
#
#   .env       — 공유 기본값
#   .env.local — 개인/머신 override (override=True 로 .env 위에 덮임)
#
# 사용법:
#   cp .env.example .env.local
#   # .env.local 을 실제 값으로 채움
#
# `.env`, `.env.local` 은 `.gitignore` 의 `.env.*` 패턴으로 차단되며
# `.env.example` 만 예외로 commit 됩니다.

# === 앱 비밀번호 게이트 (PRD app-password-gate) ===
#
# 앱 전체를 단일 공유 비밀번호로 보호한다(루트 middleware 게이트).
# ⚠️ 서버 전용 — NEXT_PUBLIC_ 접두사 금지(붙이면 브라우저 번들에 노출). KIS 키와 동일 취급.
#
# APP_PASSWORD: 공유 비밀번호.
#   - 빈 값/미설정 = 게이트 비활성(앱 전체 공개). 로컬/CI 편의.
#   - 프로덕션 필수 — 미설정 시 Vercel 프로덕션 도메인이 공개됩니다(경고 로그 1회).
#   - 16자 이상 랜덤 권고(카운터/잠금 없이 길이로 온라인 브루트포스 실효성 차단).
#     예: openssl rand -base64 24
APP_PASSWORD=
#
# APP_AUTH_SECRET: 세션 쿠키 HMAC 서명 시크릿(APP_PASSWORD 와 한 쌍).
#   - 랜덤 긴 문자열. 예: openssl rand -hex 32
#   - 회전(변경) 시 발급된 전체 세션이 무효화되어 전원 재로그인(의도된 강제 로그아웃 수단).
APP_AUTH_SECRET=

# trading-signal-engine FastAPI 주소 — Next.js route handler 가 이 값으로 프록시.
# 로컬: engine 레포에서 `ALLOW_UNAUTHENTICATED_WORKBENCH=1 PYTHON=.venv/bin/python make signal-workbench` 로 띄우면 기본값으로 LIVE.
# Vercel 배포 시 Environment Variables 에 동일한 키로 등록합니다.
FASTAPI_BASE_URL=http://127.0.0.1:8000

# === 분석 백엔드 선택 (PRD claude-cli-analysis) ===
#
# `fastapi` (기본): 위 FASTAPI_BASE_URL 의 trading-signal-engine 호출.
# `claude-cli`   : 로컬에 설치된 claude CLI 를 subprocess 로 호출하여 분석 결과를 생성.
#                  Vercel 등 serverless 환경에서는 동작하지 않으며, 런타임 가드가 명시적 에러를 반환합니다.
# 빈 값/오타는 `fastapi` 로 폴백합니다.
ANALYZE_BACKEND=fastapi

# claude CLI 바이너리 경로. PATH 에 있으면 `claude` 그대로 사용.
# 다른 경로에 설치된 경우 절대경로 지정 (예: /Users/<me>/.nvm/versions/node/v20/bin/claude).
CLAUDE_CLI_PATH=claude

# (옵션) claude CLI 의 `--model` 플래그로 전달할 모델 ID 또는 alias.
# 비워두면 claude CLI 기본 모델을 사용합니다. 분석가 그룹 권장 = claude-sonnet-5
# (Sonnet 4.6 대비 코딩·에이전틱 품질 상승, 단가 동일 $3/$15 — 2026-08-31까지 인트로 $2/$10).
# CLAUDE_CLI_MODEL=claude-sonnet-5

# === 장중 단타 경량 에이전트 그룹(cli-agent) 모델 ===
# (옵션) 단타 ①흐름·세력 분석가 / ②진입·청산 판단가 모델. 둘 다 로컬 claude CLI(구독, API 토큰 X).
# 우선순위: 에이전트별 → INTRADAY_MODEL → CLAUDE_CLI_MODEL → CLI 기본.
# ⚠️ Haiku 4.5·Sonnet 4.5 는 effort 파라미터 미지원이라 코드가 자동으로 effort 를 생략합니다
#    (Sonnet 5·4.6, Opus 계열은 effort 지원 — low/medium/high/xhigh/max).
# 빠른 분석가는 haiku, 판단가는 필요 시 sonnet 으로 올리는 식으로 분리 가능.
# INTRADAY_MODEL=claude-haiku-4-5
# INTRADAY_ANALYST_MODEL=claude-haiku-4-5
# INTRADAY_JUDGE_MODEL=claude-sonnet-5

# (옵션) 단타 판단 주기(분, 기본 5)·분봉 단위(1/3/5/15, 기본 5).
# 예: 2분마다 1분봉 판단 = INTRADAY_TICK_INTERVAL_MINUTES=2 + INTRADAY_TIMEFRAME=1.
# ⚠️ 1분봉은 페치 볼륨이 큼(KIS 30봉/콜 vs 토스 200봉/콜) — MARKET_DATA_SOURCE=toss 병행 권장.
# INTRADAY_TICK_INTERVAL_MINUTES=2
# INTRADAY_TIMEFRAME=1

# (옵션) 단타 판단(참고) Slack 알림 — incoming webhook URL. 설정 시 /api/cron/intraday-slack 이 푸시.
# 미설정이면 조용히 skip. Slack 워크스페이스 → Incoming Webhooks 에서 발급.
# INTRADAY_SLACK_WEBHOOK_URL=https://hooks.slack.com/services/XXX/YYY/ZZZ

# === 시황 자동 갱신 스케줄러(로컬 crontab 대체) ===
# `instrumentation.ts` 가 dev 서버 부팅 시 평일 09:00~15:30 KST 동안 N분마다 시황 분석을 자동 생성·적립.
# crontab(scripts/cron/refresh-market-analysis.sh) 없이 동작 → 동료는 `npm run dev` 만 켜면 됩니다.
# ⚠️ 시황은 공유 Supabase 에 append 되므로 **지정 호스트 한 명만** =1 로 켭니다(다인 동시 활성 시 중복).
# ⚠️ CLI 합성 전용이라 로컬에서만 의미. Vercel(서버리스)에서는 자동 no-op.
# MARKET_REFRESH_SELF_SCHEDULE=1
# (옵션) 갱신 주기(분). 기본 30. 소비 측 24h 신선도 가드 기준 하루 1회면 충분하므로 늘려도 됩니다.
# MARKET_REFRESH_INTERVAL_MIN=30

# (옵션) user prompt 본문 템플릿 override. 비우면 기본 한글 템플릿을 사용합니다.
# 지원 placeholder: {{ticker}}, {{capital_amount}}, {{target_return_pct}}, {{target_period_days}}, {{max_loss_pct}}
# CLAUDE_PROMPT_TEMPLATE=

# === 종목 상세 AI 종합분석 CLI ===
#
# Codex CLI 바이너리 경로. PATH에 있으면 `codex` 그대로 사용.
# Codex 앱 번들 CLI 예: /Applications/Codex.app/Contents/Resources/codex
CODEX_CLI_PATH=codex

# (옵션) `codex --model`로 전달할 모델 ID. 비우면 Codex CLI 기본 모델을 사용합니다.
# CODEX_CLI_MODEL=

# Codex 분석의 작업 디렉터리. 저장소 AGENTS.md와 코드 컨텍스트를 불필요하게 읽지 않도록
# 프로젝트 밖의 임시 디렉터리를 사용합니다.
CODEX_CLI_WORKDIR=/tmp

# === Supabase (AI 분석 최신 결론 공유 저장) ===
#
# `ai_analysis_decisions` 테이블에 ticker 별 Portfolio Manager 최신 결론 1건을 upsert 합니다.
# 미설정 시 분석은 계속 가능하며, 이전 결론 조회/저장만 건너뜁니다.
# ⚠️ SUPABASE_SERVICE_ROLE_KEY 는 서버 전용 — NEXT_PUBLIC_ 접두사 금지.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# === 자가교정 피드백 (PRD scorecard-feedback (나)) ===
#
# PM 분석 프롬프트에 과거 판정 성적(채점 적중률)을 주입할지 여부 — **기본 OFF**.
# 미설정/그 외 값 = OFF → PM 프롬프트 무변경(완전 무회귀). "1"·"true"·"on" 이면 ON.
# 운영자가 채점 표본(n>=20 버킷)을 충분히 신뢰한 뒤에만 켭니다. 서버 전용(NEXT_PUBLIC_ 금지).
# SCORECARD_FEEDBACK_PROMPT=

# 참고(PRD market-context-injection, Phase 3): 종목분석(market·news·portfolio_manager)
# 프롬프트에 저장된 최신 시황 분석(/api/market/analysis 의 ?mode=latest 본)을 시장 국면 맥락으로
# **항상 주입**한다(별도 env 토글 없음). 저장본 읽기 1회·CLI 0콜이라 종목분석 latency 무증가.
# 저장본이 24h 초과로 묵으면 자동 skip(신선도 가드), 미설정/조회실패/저장본없음도 skip(fail-soft).

# === KIS 토큰/지수 공유 store (PRD kis-token-store) ===
#
# KIS 키와 store 연결 변수의 상세 안내는 `.env.local.example` 참고.
#   KIS_TOKEN_STORE: "memory"(기본/폴백) | "kv"(Upstash Redis 공유 store).
#   ⚠️ 서버 전용(NEXT_PUBLIC_ 금지). Vercel Marketplace Upstash 연결 시 자동 주입,
#   미설정 시 memory 폴백이라 머지/실행에 무해(fail-soft).
# KIS_TOKEN_STORE=memory
# KV_REST_API_URL=            # 또는 UPSTASH_REDIS_REST_URL (둘 다 흡수)
# KV_REST_API_TOKEN=          # 또는 UPSTASH_REDIS_REST_TOKEN

# (선택) 로그 레벨 — DEBUG / INFO / WARNING / ERROR
LOG_LEVEL=INFO
